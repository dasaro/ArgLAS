# Legacy run configs

Pre-audit campaign configs kept for the historical record only: they carry **no pinned
seeds**, so they are not reproducible and are not part of the paper's evaluation — use
the seeded configs in `experiments/run_configs/` instead.
